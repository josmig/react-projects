import React from 'react';    // #1 debemos importar siempre la libreria para usar todo sobre React

function Header(){            // #2 creación de una función + return ahi se coloca lo que se ve en pantalla
    return(
        <h1>Desde el header</h1>
    )
}

export default Header;